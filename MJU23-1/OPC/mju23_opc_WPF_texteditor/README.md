# opc1_d11_WPF_texteditor

Lösning på OPC dag 11 övning 1: gör en texteditor med WPF.
Om du vill studera git-history på bästa sätt, gör en `git clone`
och starta `gitk` i bash eller från kommandoraden!